import XCTest
@testable import WorkflowExampleLibrary

final class WorkflowExampleLibraryTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        //XCTAssertEqual(WorkflowExampleLibrary().text, "Hello, World!")
    }
}
